namespace SpaceSurvivalGame.ECS.Components;

/// <summary>Marks an entity as an asteroid. Minimal for now — natural place to add resource type/health once mining exists.</summary>
public struct Asteroid
{
    public float RadiusMeters;
}
